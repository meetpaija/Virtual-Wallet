<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
xmlns:app="http://schemas.android.com/apk/res-auto"
android:orientation="vertical" android:layout_width="match_parent"
android:layout_height="match_parent"
android:padding="20dp">

<ImageView
    android:layout_width="70dp"
    android:layout_height="70dp"
    android:contentDescription="profile_img"
    app:srcCompat="@drawable/noavatar"
    android:id="@+id/profile_img_notify" />


<TextView
    android:layout_width="wrap_content"
    android:layout_height="30dp"
    android:textSize="17dp"
    android:layout_marginLeft="20dp"
    android:textColor="@color/black"
    android:layout_toRightOf="@+id/profile_img_notify"
    android:id="@+id/user_mobile_notify"/>

<View
    android:layout_width="match_parent"
    android:layout_height="1dp"
    android:layout_marginLeft="20dp"
    android:background="@color/black"
    android:layout_toRightOf="@+id/profile_img_notify"
    android:layout_below="@+id/user_mobile_notify"
    android:id="@+id/view1_notify">

</View>

<TextView
    android:layout_width="wrap_content"
    android:layout_height="40dp"
    android:layout_below="@+id/profile_img_notify"
    android:id="@+id/datetime_notify"/>

<TextView
    android:layout_width="wrap_content"
    android:layout_height="40dp"
    android:layout_marginLeft="20dp"
    android:layout_toRightOf="@+id/profile_img_notify"
    android:layout_below="@+id/view1_notify"
    android:id="@+id/content_notify"/>


</RelativeLayout>